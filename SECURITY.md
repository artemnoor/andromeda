# Security

Do not commit credentials, cookies, database URLs with passwords, Telegram tokens or user profiles. Use environment variables or the deployment secret store.

Report security issues privately to the project maintainers rather than opening a public issue with exploit details. The supported security baseline is the current `MVP / Private Alpha` release on `main`.
