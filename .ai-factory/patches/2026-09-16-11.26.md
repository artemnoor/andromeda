# CI strict JSON enum boundary

## Symptom

The clean GitHub Actions runner resolved Pydantic 2.13.5 while the local
environment still used Pydantic 2.10.6. With the project's strict API models,
valid JSON strings for enum fields were rejected with HTTP 422: first the
analytics endpoint rejected `eventType`, then the browser flow exposed the same
problem for the session answer `status`.

## Root cause

The request schema relied on older Pydantic behavior for strict enum fields.
The public JSON contract contains enum values as strings, so that conversion
must be explicit and deterministic in the API schema rather than delegated to
version-specific coercion.

## Prevention

- Use a `BeforeValidator` for every enum field that receives JSON strings while
  keeping `ApiModel(strict=True, extra="forbid")` enabled; do not patch only the
  first endpoint that happens to fail.
- Re-run the full backend suite against the newest supported dependency
  versions when a clean CI runner exposes a validation drift.
- Keep HTTP-boundary regression tests for analytics and resumable session
  requests, alongside strict payload rejection tests.

Tags: #ci #pydantic #strict-validation #api #regression
